# Optional: enables shared Supabase persistence and Realtime synchronization.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: prevents casual access to the results setup page.
# This value is visible in the browser bundle and is not strong authentication.
VITE_SETUP_PASSCODE=
